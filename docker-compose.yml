services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-xyence}
      POSTGRES_USER: ${POSTGRES_USER:-xyence}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-xyence}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build:
      context: ./backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      POSTGRES_HOST: db
      XYN_SEED_BASE_URL: ${XYN_SEED_BASE_URL:-http://host.docker.internal:8001/api/v1}
      XYN_SEED_API_TOKEN: ${XYN_SEED_API_TOKEN:-}
    depends_on:
      - db
    ports:
      - "8000:8000"
    volumes:
      - backend_media:/app/media
      - backend_static:/app/staticfiles
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    ports:
      - "8080:80"

volumes:
  postgres_data:
  backend_media:
  backend_static:
